<form method="post" action="{{ path('app_articles_listes_delete', {'id': articles_liste.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ articles_liste.id) }}">
    <button class="btn">Delete</button>
</form>
