{% extends 'base.html.twig' %}

{% block title %}Modifier Produit{% endblock %}

{% block main_content %}
    <div class="max-w-3xl mx-auto mt-10 p-8 bg-white shadow-xl rounded-lg">
        <!-- Titre principal -->
        <h1 class="text-3xl font-extrabold text-center mb-8 text-gray-800">⚙️ Modifier le Produit</h1>

        <!-- Formulaire d'édition -->
        {{ form_start(form, { 'attr': { 'class': 'space-y-6' } }) }}
        <!-- Champ Nom -->
        <div>
            <label for="produit_nom" class="block text-gray-700 font-semibold mb-2">Nom du Produit</label>
            {{ form_widget(form.nom, {'attr': {'class': 'input input-bordered w-full focus:ring-2 focus:ring-blue-400', 'placeholder': 'Ex: Pommes Golden'}}) }}
        </div>

        <!-- Champ Marque -->
        <div>
            <label for="produit_marque" class="block text-gray-700 font-semibold mb-2">Marque</label>
            {{ form_widget(form.marque, {'attr': {'class': 'input input-bordered w-full focus:ring-2 focus:ring-blue-400', 'placeholder': 'Ex: Bio Village'}}) }}
        </div>

        <!-- Champ Quantité -->
        <div>
            <label for="produit_quantite" class="block text-gray-700 font-semibold mb-2">Quantité</label>
            {{ form_widget(form.quantite, {'attr': {'class': 'input input-bordered w-full focus:ring-2 focus:ring-blue-400', 'placeholder': 'Ex: 5'}}) }}
        </div>

        <!-- Champ Commentaires -->
        <div>
            <label for="produit_commentaires" class="block text-gray-700 font-semibold mb-2">Commentaires</label>
            {{ form_widget(form.commentaires, {'attr': {'class': 'textarea textarea-bordered w-full focus:ring-2 focus:ring-blue-400', 'placeholder': 'Ajoutez des détails sur le produit'}}) }}
        </div>

        <!-- Boutons d'action -->
        <div class="flex justify-between items-center mt-6">
            <!-- Bouton Retour -->
            <a href="{{ path('app_produit_index') }}" class="btn btn-outline btn-secondary">
                ↩️ Retour à la liste
            </a>

            <!-- Bouton Soumettre -->
            <button type="submit" class="btn btn-primary px-6 py-2">
                💾 Mettre à jour
            </button>
        </div>
        {{ form_end(form) }}

        <!-- Formulaire de suppression -->
        <div class="mt-8 text-center">
            {{ include('produit/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
