<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Liste de Courses{% endblock %}</title>
    <!-- Importation TailwindCSS -->
    <link href="{{ asset('css/tailwind.css') }}" rel="stylesheet">
    {% block stylesheets %}{% endblock %}
</head>
<body class="bg-gray-100 font-receipt text-sm">

    <!-- Barre de navigation -->
    <nav class="navbar bg-gray-800 text-white p-4">
        <div class="container mx-auto flex justify-between items-center">
            <a href="{{ path('zone_index') }}" class="text-xl font-bold">🛒 Mes Courses</a>
            <ul class="flex space-x-4">
                <li><a href="{{ path('zone_index') }}" class="hover:text-gray-400">Zones</a></li>
                <li><a href="{{ path('app_produit_index') }}" class="hover:text-gray-400">Produits</a></li>
                <li><a href="{{ path('app_liste_index') }}" class="hover:text-gray-400">Listes</a></li>
            </ul>
        </div>
    </nav>

    <!-- Contenu principal -->
    <div class="container mx-auto mt-8 p-4 bg-white shadow-lg rounded">
        {% block main_content %}
        {% endblock %}
    </div>

    <!-- Pied de page -->
    <footer class="text-center p-4 mt-8 text-gray-500 text-xs">
        <p>&copy; 2024 Liste de Courses - Tous droits réservés</p>
    </footer>

    {% block javascripts %}
    {% endblock %}
</body>
</html>
